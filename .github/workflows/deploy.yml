name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Instalar dependências
        run: npm install
        working-directory: .

      - name: Rodar app
        run: npm start
        working-directory: .
